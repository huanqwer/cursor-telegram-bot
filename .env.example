# Telegram Bot 环境变量示例
# 复制本文件为 .env 并填写真实值，切勿将 .env 提交到版本控制

# Bot Token（必填）：从 @BotFather 获取
TELEGRAM_BOT_TOKEN=

# 是否使用代理访问 Telegram API（可选，默认 true）
USE_PROXY=true

# 代理地址（可选，默认 http://127.0.0.1:7890）
PROXY_URL=http://127.0.0.1:7890

# 默认项目根路径（可选）：未指定 --project 且无会话记忆时使用
# DEFAULT_PROJECT_ROOT=/path/to/your/project

# Cursor CLI 可执行路径（可选，默认使用 PATH 中的 agent）
# CURSOR_AGENT_PATH=/path/to/agent
